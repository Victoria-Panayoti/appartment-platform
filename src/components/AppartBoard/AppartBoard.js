import css from "./AppartBoars.module.css"

export default function AppartBoard({ apparts }) {
    console.log(apparts)
    return (
        <div className={css.appartBoard}>AppartCard

        </div>
    )
}